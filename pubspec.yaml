name: hotel
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  bloc: ^8.1.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  carousel_slider: ^4.2.1
  flutter:
    sdk: flutter
  get_it: ^7.6.0
  flutter_svg: ^2.0.7
  masked_text: ^1.1.2
  injectable: ^2.3.0
  dio: ^5.3.3
  flutter_dotenv: ^5.1.0
  


dev_dependencies:
  injectable_generator: ^2.4.0
  build_runner: ^2.4.6
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.2

flutter:
  assets:
      - assets/
      - assets/png/paid_for.png
      - assets/env/.env
  uses-material-design: true
